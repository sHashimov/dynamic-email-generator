# dynamic-email-generator
A secure and scalable Spring Boot service that dynamically generates customized email addresses based on user-defined expressions and input parameters. Includes a custom expression language, RESTful API, Docker deployment with NGINX + SSL, and full documentation.
